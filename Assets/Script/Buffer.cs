using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using JSNodeMap;

public class Buffer : MonoBehaviour
{
   /*
    //Variables for Buffer
    [SerializeField] private Node node;
    [SerializeField] private int count;
    [SerializeField] private int bufferLimit;
    //private List bufferList;


    private void Update()
    {
        
    }
    private void OnCollisionEnter(Collision collision)
    {
        if(collision.gameObject.tag == "packet")
        {
            if(count + 1<= bufferLimit)
            {
                //bufferList.Add(collision);
            }
            else
            {
                string goName= collision.gameObject.name;
                SendNack(goName);
                Destroy(collision.gameObject);
            }
        }
    }
    private void SendNack(string goName)
    {

    }
   */
}
